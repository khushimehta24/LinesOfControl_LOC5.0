from django.urls import path
from . import views

# urlpatterns = [
#     path('folow-request/', views..as_view(), name = 'follow-request'),
#     path('follow-accept/',views..as_view(), name = 'follow-accept' )

urlpatterns =[]